import { makeStyles } from "@mui/styles";

export default makeStyles ((theme) => ({
    mainContainer: {
        background: 'white',
        borderRadius: '8px',
        padding: '20px',
    },
    header: {
        display: 'flex',
        marginBottom: "30px",
        marginTop: '30px'
    },
    
}));